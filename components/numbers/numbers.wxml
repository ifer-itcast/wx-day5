<view>
  {{numA}} + {{numB}} = {{sum}}
</view>

<van-button type="primary" bindtap="btnHandler2" data-step="{{1}}">+1</van-button>
<van-button type="danger" bindtap="btnHandler2" data-step="{{-1}}">-1</van-button>